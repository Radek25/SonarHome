import { FC } from 'react';

import { LaunchesWrapper } from './LaunchesPageStyle';

export const LaunchesPage : FC = () => {
    return(
        <LaunchesWrapper>
        </LaunchesWrapper>
    );
};